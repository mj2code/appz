<%= form_for(@appztble) do |f| %>
  <% if @appztble.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appztble.errors.count, "error") %> prohibited this appztble from being saved:</h2>

      <ul>
      <% @appztble.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
